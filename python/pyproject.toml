[project]
name = "mcp-servers"
version = "0.1.0"
description = "Common MCP servers written in Python"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.0.2",
    "pandas>=3.0.1",
    "pytest>=9.0.2",
    "requests>=2.32.5",
    "rich>=14.3.3",
]
